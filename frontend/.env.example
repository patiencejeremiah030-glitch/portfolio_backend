# Local dev
VITE_API_URL=http://127.0.0.1:8000/api

# Vercel (Production) — set in Project → Settings → Environment Variables, then Redeploy
# VITE_API_URL=https://portfolio-api.onrender.com/api

# Google Analytics 4 — Measurement ID from GA4 property (Admin → Data streams → Web)
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
